class Main {

    field int test;
    static int check;

    constructor Main new () {
        var int a;
        return this;
    }

    method int main () {
        var int x, y, z;
        let z = x + (Main.g(2, y, -z) * (-5));
        do Main.g(1,2,3);
        return z;
    }

    function int g(int a, int b, int c) {
        if (a > 1) {
            if (b > 1) {
                if (c > 1) {
                    return 0;
                }
                else {
                    return 1;
                }
            }
            else {
                return 2;
            }
        }
        else {
            return 3;
        }
    }

    method int test () {
        let test = 100;
        return test;
    }

    function void test2 () {
        var int a;
        var Array b; 
        let b[0] = a;
        return;
    }
}